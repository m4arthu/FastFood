import { OrderUsernameContent } from "../styles.css/order.style"

export const  OrderUsername =({finished}) => {
    return (
        <OrderUsernameContent finished={finished}>name</OrderUsernameContent>
    )
}